__pycache__/
*.pyc

# Secrets — never commit
.env
*.env
.env.*

# Downloaded datasets (fetched via datasets/download.sh) — too large / licence-restricted to commit
datasets/data/

# AVM trained model binary (regenerate: python3 avm/train_avm.py)
avm/artifacts/*.joblib
