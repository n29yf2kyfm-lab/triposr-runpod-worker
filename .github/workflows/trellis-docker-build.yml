name: Build and Push TRELLIS Docker Image

on:
  push:
    branches: [ main ]
    paths:
      - 'trellis/**'
      - '.github/workflows/trellis-docker-build.yml'
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # This image (nvidia/cuda:12.1.0-devel base + torch CUDA wheels + compiled
      # extensions) does not fit in the ~14GB free on a stock ubuntu-latest runner.
      # Strip GitHub's preinstalled SDKs we don't need before the build even starts.
      - name: Free disk space
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          swap-storage: true
          docker-images: false

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: trellis
          push: true
          tags: |
            alamk123/ai-mechanic:trellis-v1
            alamk123/ai-mechanic:trellis-latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
